export class Klok {
    constructor(name, timeZone, locale) {
        this.name = name;
        this.timeZone = timeZone;
        this.locale = locale;
    }

    name = "Thuis";
    timeZone = "Europe/Brussels";
    locale = "nl-BE";
}